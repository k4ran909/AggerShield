# detector.py (--backend ewma) needs NONE of these — it is pure standard library.
# These are only for the ML model path: training (train_model.py) and
# detector.py --backend model.
numpy
pandas
scikit-learn
joblib
xgboost
